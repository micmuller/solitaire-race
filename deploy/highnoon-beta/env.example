# Compose interpolation only; no values are passed wholesale into containers.
# Install as /srv/micnet/secrets/highnoon-beta.env, root:root 0600.
# Use docker image inspect IDs from build.sh (sha256:...), never :latest.
HIGHNOON_APP_IMAGE=
HIGHNOON_ORIGIN_IMAGE=
